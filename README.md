# Cluster Tools

Small collection of utility scripts for working on the cluster.

## Install

Clone the repo in the cluster:

```bash
cd /mnt/deadmanschest/pxpalacios
git clone https://github.com/ppalacios92/cluster.git